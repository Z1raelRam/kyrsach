# Application settings
spring.application.name=hostel-api

# Disable Docker Compose (just in case)
spring.docker.compose.enabled=false

# PostgreSQL DataSource Settings
spring.datasource.url=jdbc:postgresql://localhost:5432/kyrsach
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

# Flyway Migration Settings
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

jwt.expiration=3600000
jwt.refresh.expiration=604800000

spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jackson.time-zone=UTC

# OAuth2 GitHub Settings
spring.security.oauth2.client.registration.github.client-id=Ov23lii1Bz6H7nEtxSOS
spring.security.oauth2.client.registration.github.client-secret=9788c4129698f878aa778227006d41f1e7e72fa4

spring.data.redis.host=localhost
spring.data.redis.port=6379